import React from 'react';

export const Header = (props) => {
  const styles = {
    backgroundColor: '#1A1C1D',
    display: 'flex',
    flexDirection: 'row',
    minHeight: 50,
    position: 'fixed',
    top: 0,
  };

  return (
    <div style={styles}>
    </div>
  );
};